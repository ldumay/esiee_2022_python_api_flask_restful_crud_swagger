```
python3 -m venv venv
source venv/bin/activate

py -m venv venv
. .\venv\Scripts\activate

touch requirements.txt

pip install --upgrade pip
pip install -r requirements.txt

pip install flask
pip install flask-restful
pip install flask-sawgger-ui
```